import Button from "react-bootstrap/Button";
import "bootstrap/dist/css/bootstrap.min.css";

const MyButton = (props) => {
  return (
    <div className="">
      <Button className="inter-medium navButton" active>
        Button
      </Button>
    </div>
  );
};

export default MyButton;

